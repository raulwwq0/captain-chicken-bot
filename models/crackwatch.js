'use strict'

const embedMessage = {
    color: String,
    title: String,
    url: String,
    thumbnail: {
        url: String,
    },
    description: String,
    gameFound: false,
    counter: 0,
}

module.exports = embedMessage;